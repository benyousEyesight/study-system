server:
  port: 8082

spring:
  application:
    name: question-service
  datasource:
    url: jdbc:mysql://localhost:3307/question_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
