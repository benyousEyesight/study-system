server:
  port: 8082

spring:
  application:
    name: question-service
  datasource:
    url: jdbc:mysql://localhost:3306/question_db?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.study.question.model.entity
  global-config:
    db-config:
      id-type: auto
