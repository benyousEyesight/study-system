server:
  port: 8083
spring:
  application:
    name: paper-service
  datasource:
    url: jdbc:mysql://localhost:3307/paper_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.study.paper.model.entity
  global-config:
    db-config:
      id-type: auto